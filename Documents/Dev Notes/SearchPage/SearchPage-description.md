# 🐾 Search Page Feature – Dev Notes

## คำอธิบายฟีเจอร์ (Feature Description)

ฟีเจอร์นี้ช่วยให้ผู้ใช้งานสามารถ **ค้นหา Pet Sitter** ตามเงื่อนไขที่กำหนดได้ เช่น ประเภทสัตว์เลี้ยง คะแนนรีวิว หรือประสบการณ์การทำงาน ทำให้กระบวนการเลือกมีประสิทธิภาพมากขึ้น และตอบโจทย์ความต้องการเฉพาะบุคคล

---

## ✅ Checklist การพัฒนา (Development Checklist)

### 🖥️ UI / Interface
- [ ] พัฒนาหน้า Search Page แสดงผลรายการ Pet Sitter
- [ ] ทำ Search Bar / Input สำหรับกรอกเงื่อนไขการค้นหา
- [ ] ทำ Filter Panel สำหรับเลือกเงื่อนไขกรอง
- [ ] ทำ Sort Dropdown สำหรับเรียงลำดับผลลัพธ์
- [ ] ทำ Responsive Layout รองรับทุกขนาดหน้าจอ

### 🔍 ฟังก์ชันกรองข้อมูล (Filter)
- [ ] กรองตาม **ประเภทสัตว์เลี้ยง** (Dog, Cat, Bird, Rabbit, ...)
- [ ] กรองตาม **คะแนนรีวิว** (Rating)
- [ ] กรองตาม **ประสบการณ์** (Experience)

### 📊 ฟังก์ชันเรียงลำดับ (Sorting)
- [ ] เรียงตามคะแนนรีวิว (Rating: สูง → ต่ำ / ต่ำ → สูง)
- [ ] เรียงตามประสบการณ์ (Experience)

### 🃏 การแสดงผล Pet Sitter Card
- [x] แสดงชื่อสถานที่ (Title)
- [x] แสดงชื่อผู้ดูแลสัตว์เลี้ยง (Pet Sitter Name)
- [x] แสดงคะแนนรีวิว (Rating Stars)
- [x] แสดงสถานที่ตั้ง (Location)
- [x] แสดงประเภทสัตว์เลี้ยงที่รับดูแล (Pet Type Tags)
- [x] แสดงรูปภาพ (Image)

### 🔗 Data Layer
- [x] สร้าง `usePetSitterData` custom hook สำหรับดึงข้อมูล
- [x] กำหนด `PetSitter` interface (`id`, `title`, `name`, `rating`, `location`, `petTypes`, `image`)
- [x] เพิ่ม Mock Data 5 รายการสำหรับทดสอบ
- [ ] เชื่อมต่อ API จริงแทน Mock Data

### 🧪 การทดสอบ (Testing)
- [ ] Usability Testing – ทดสอบความใช้งานง่ายของ UI
- [ ] ทดสอบ Filter และ Sort ว่าทำงานถูกต้อง
- [ ] ทดสอบบน Browser ต่าง ๆ (Chrome, Safari, Firefox)
- [ ] ทดสอบบนอุปกรณ์ Mobile / Tablet

---

## 📌 User Acceptance Criteria

| # | เกณฑ์ | สถานะ |
|---|-------|-------|
| 1 | ผู้ใช้กรองตามประเภทสัตว์เลี้ยง / คะแนน / ประสบการณ์ได้ | 🔲 TODO |
| 2 | ผลค้นหาแสดงชื่อ สถานที่ และคะแนนรีวิวของ Pet Sitter | ✅ Done |
| 3 | ผู้ใช้สามารถเรียงลำดับผลค้นหาได้ | 🔲 TODO |
| 4 | ใช้งานได้บนอุปกรณ์และ Browser หลากหลาย | 🔲 TODO |
| 5 | UI ใช้งานง่าย เข้าใจง่าย ไม่ซับซ้อน | 🔲 TODO |

---

## 📝 สิ่งที่ทำเสร็จแล้ว (Done So Far)

- **`PetSitter` interface** – กำหนด type ครบถ้วนใน `usePetSitterData.ts` รวมถึง `title` และ `petTypes`
- **`usePetSitterData` hook** – ดึงข้อมูลแบบ async พร้อม mock data 5 รายการ
- **`PetSitterCard` component** – แสดงข้อมูล Pet Sitter ในรูปแบบ card (รูป, ชื่อ, rating, location, tags)
- **`ListMode` component** – วน render `PetSitterCard` จาก hook data (แก้ conflict ของ local `PetSitter` type แล้ว)
- **Type conflict fix** – ลบ local `PetSitter` interface ใน `ListMode.tsx` ออก เพื่อให้ใช้ type เดียวจาก hook

---

## 🗂️ ไฟล์ที่เกี่ยวข้อง (Related Files)

| ไฟล์ | หน้าที่ |
|------|---------|
| `src/hooks/usePetSitterData.ts` | Interface + Mock Data + Custom Hook |
| `src/components/search/PetSitterCard.tsx` | Card UI Component |
| `src/components/search/ViewMode/ListMode.tsx` | List layout ที่แสดง cards |
| `src/components/search/PetSitterCard/LocationPetSitter.tsx` | Sub-component แสดง Location |
| `src/app/search/page.tsx` | หน้า Search Page หลัก |
