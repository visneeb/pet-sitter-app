# Known Issue: CustomSelect Dropdown Overflow (ล้นจอด้านล่าง)

**Component:** `CustomSelect.tsx`  
**ใช้ใน:** `FilterExperience.tsx` → `FilterSideBar.tsx`  
**วันที่พบ:** 2026-02-21  
**Status:** 🔴 Open

---

## อาการ

เมื่อกดเปิด `CustomSelect` ที่อยู่ใกล้ด้านล่างของ sidebar แล้ว dropdown list (`<ul>`) จะล้นออกนอก viewport ด้านล่าง ทำให้ผู้ใช้ไม่สามารถ scroll ลงไปเลือก option ได้

## สาเหตุ

`CustomSelect` ใช้ `position: absolute` บน `<ul>` โดย render dropdown **ลงด้านล่างเสมอ** (`mt-1` = top: 100%)

```tsx
// CustomSelect.tsx บรรทัด 83
<ul className="absolute z-10 w-full mt-1 ...">
```

ประกอบกับ `FilterSideBar` ใช้ `sticky top-20 h-fit` ทำให้ scroll ตามลงไปไม่ได้

---

## วิธีแก้ที่เป็นไปได้

### วิธีที่ 1: Drop-up เสมอ (Quick Fix) ⭐ ง่าย

เปลี่ยน dropdown ให้เปิดขึ้นด้านบนตลอด

```diff
- <ul className="absolute z-10 w-full mt-1 ...">
+ <ul className="absolute z-10 w-full bottom-full mb-1 ...">
```

**ข้อดี:** แก้ง่าย ไม่ต้องใช้ JS เพิ่ม  
**ข้อเสีย:** เปิดขึ้นบนเสมอ แม้จะมีที่ว่างด้านล่างก็ตาม

---

### วิธีที่ 2: Smart Positioning (Best UX) ⭐⭐ แนะนำ

ใช้ `getBoundingClientRect()` ตรวจสอบ space ด้านล่างแล้วตัดสินใจทิศทางเอง

```tsx
const [openUpward, setOpenUpward] = useState(false);
const buttonRef = useRef<HTMLButtonElement>(null);

const handleOpen = () => {
  if (buttonRef.current) {
    const rect = buttonRef.current.getBoundingClientRect();
    const spaceBelow = window.innerHeight - rect.bottom;
    setOpenUpward(spaceBelow < 150);
  }
  setIsOpen(!isOpen);
};
```

```tsx
<ul className={cn(
  "absolute z-10 w-full ...",
  openUpward ? "bottom-full mb-1" : "mt-1"
)}>
```

**ข้อดี:** ปรับทิศทางอัตโนมัติตาม position จริง — Production-quality  
**ข้อเสีย:** ต้องแก้ `CustomSelect.tsx` เพิ่มเติม

---

### วิธีที่ 3: Native `<select>` (Not Recommended)

ใช้ native HTML `<select>` แทน — browser จัดการ overflow เอง  
**ข้อเสีย:** เสีย custom styling ทั้งหมด ❌

---

## สรุปคำแนะนำ

| วิธี | ความยาก | UX |
|---|---|---|
| 1. Drop-up เสมอ | ง่าย | ดี |
| **2. Smart Position** | กลาง | **ดีที่สุด** ✅ |
| 3. Native select | ง่าย | พอใช้ |

> **แนะนำ วิธีที่ 2** เพราะ `CustomSelect` ถูกใช้ในหลายที่ การแก้ที่ component เดียวจะ fix ทุก use case โดยอัตโนมัติ
